# ───────────────────────── Core ─────────────────────────
pydantic>=2.7          # Config / validation models
httpx>=0.27            # Outbound HTTP to OpenAI

# ───────────────────────── gRPC stack ───────────────────
grpcio>=1.64           # Server + client runtime
grpcio-reflection>=1.64        # Enables Postman / grpcurl reflection
grpcio-health-checking>=1.64   # Standard health‑check service
protobuf>=4.25         # Generated *pb2 files

# ───────────────────────── FastAPI gateway (if you keep it) ──
fastapi>=0.111
uvicorn[standard]>=0.29        # ASGI server, reload, etc.

# ───────────────────────── Utilities ────────────────────
python-dotenv>=1.0     # Load env vars from .env (optional)
tenacity>=8.2          # If your circuit_breaker decorator wraps Tenacity

# ───────────────────────── Dev / test (optional) ────────
# pytest>=8.1
# pytest-asyncio>=0.23
# black>=24.4
# isort>=5.13
# mypy>=1.10